import { FournisseurPipe } from './fournisseur.pipe';

describe('FournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});
